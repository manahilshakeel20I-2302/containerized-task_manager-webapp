version: "3.9"

services:

  backend:
    build: ./backend
    container_name: task-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: ./frontend/myapp
    container_name: task-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always